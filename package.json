{
  "name": "kafka-etl",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "lerna run compile",
    "dev": "yarn run compile && lerna run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2",
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
