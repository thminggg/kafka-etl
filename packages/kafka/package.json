{
  "name": "@thminggg/kafka",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "author": "Patrick T",
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "yarn run clean && tsc",
    "kafka": "docker compose -f docker-compose.yml up -d",
    "topic:create": "yarn run compile && node ./dist/create-topic.js",
    "produce": "yarn run compile && node ./dist/producer.js",
    "consume": "yarn run compile && node ./dist/consumer.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "kafkajs": "^2.2.4"
  }
}
